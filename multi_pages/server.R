library(shiny)

# Define server logic required to draw a histogram
shinyServer(function(input, output) {
#   output$lm_file <- renderUI({
#     tags$iframe(
#       seamless = "seamless",
#       src = runApp("lm_file.R"))
#   })
  })
